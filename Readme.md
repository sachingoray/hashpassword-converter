#HashPassword Converter

THis is the small and simple program which use the SHA-256 algorithm, which converters the password given by 
user into the hashing password

#how to USE

1. first clone this repo into your terminal 
2. Run the script
3. Enter the password you want to get changed
